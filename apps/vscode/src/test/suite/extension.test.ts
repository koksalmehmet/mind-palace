import * as assert from 'assert';

describe('Extension Tests', () => {
  it('sample test', () => {
    assert.strictEqual(1 + 1, 2);
  });

  it('extension module test', () => {
    assert.ok(true);
  });
});
